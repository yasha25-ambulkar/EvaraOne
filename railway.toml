[build]
buildCommand = "npm install && cd backend && npm install && cd ../client && npm install && npm run build"

[deploy]
startCommand = "cd backend && NODE_ENV=production node --openssl-legacy-provider src/server.js"
healthCheckPath = "/api/v1/health"
healthCheckTimeout = 120
restartPolicyType = "on_failure"
