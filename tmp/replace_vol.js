const fs = require('fs'); let c = fs.readFileSync('D:/17-04-26/main/client/src/pages/EvaraTankAnalytics.tsx', 'utf8'); c = c.replace(/\\{Math\\.round\\(metrics\\.volumeLitres\\)\\.toLocaleString\\(\\)\\}/g, '{Math.round(latestPoint?.volume ?? metrics.volumeLitres).toLocaleString()}'); const start = c.indexOf('const chartDataForDisplay = useMemo(() => { const data ='); const end = c.indexOf('}, [filteredChartData, activeTelemetry, metrics]);') + 50; if(start>-1) c = c.substring(0, start) + 'const chartDataForDisplay = filteredChartData;' + c.substring(end); fs.writeFileSync('D:/17-04-26/main/client/src/pages/EvaraTankAnalytics.tsx', c); console.log('Replaced');
