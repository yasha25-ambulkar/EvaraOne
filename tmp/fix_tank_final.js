const fs = require('fs'); let c = fs.readFileSync('D:/17-04-26/main/client/src/pages/EvaraTankAnalytics.tsx', 'utf8'); const idx = c.indexOf('const chartDataForDisplay = filteredChartData;'); if(idx>-1){ let injection = 'const chartDataForDisplay = filteredChartData;\\n\\n      // Tank card uses ORIGINAL smoothed chartData (not interpolated), so it always shows the true latest point\\n      const smoothedLatestPoint = chartData.length > 0 ? chartData[chartData.length - 1] : null;'; c = c.substring(0, idx) + injection + c.substring(idx + 'const chartDataForDisplay = filteredChartData;'.length); } c = c.split('const pct = (metrics.percentage').join('const pct = (smoothedLatestPoint?.level ?? metrics.percentage'); fs.writeFileSync('D:/17-04-26/main/client/src/pages/EvaraTankAnalytics.tsx', c); console.log('Applied smoothed chart fix');
