const fs = require('fs'); let c = fs.readFileSync('D:/17-04-26/main/client/src/pages/EvaraTankAnalytics.tsx', 'utf8'); c = c.split('const latestPointForTankCard = chartData.length > 0 ? chartData[chartData.length - 1] : null;').join(''); c = c.split('const pct = (latestPointForTankCard ? (latestPointForTankCard.level ?? metrics.percentage)').join('const pct = (metrics.percentage'); c = c.split('Math.round(latestPointForTankCard?.volume ?? metrics.volumeLitres)').join('Math.round(metrics.volumeLitres)'); fs.writeFileSync('D:/17-04-26/main/client/src/pages/EvaraTankAnalytics.tsx', c); console.log('Reverted to real-time tank data');
