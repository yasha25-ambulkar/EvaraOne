const fs = require('fs'); let c = fs.readFileSync('D:/17-04-26/main/client/src/pages/EvaraTankAnalytics.tsx', 'utf8'); const idx1 = c.indexOf('const latestPoint = chartDataForDisplay.length > 0'); const idx2 = c.indexOf('const pct = (latestPoint'); if(idx1>-1 && idx2>-1){ let newCode = 'const latestPointForTankCard = chartData.length > 0 ? chartData[chartData.length - 1] : null; const latestPoint = chartDataForDisplay.length > 0 ? chartDataForDisplay[chartDataForDisplay.length - 1] : null;'; c = c.substring(0, idx1) + newCode + c.substring(idx1 + 'const latestPoint = chartDataForDisplay.length > 0 ? chartDataForDisplay[chartDataForDisplay.length - 1] : null;'.length); } c = c.split('const pct = (latestPoint ?').join('const pct = (latestPointForTankCard ?').split('(latestPoint.level ?? metrics.percentage)').join('(latestPointForTankCard.level ?? metrics.percentage)'); fs.writeFileSync('D:/17-04-26/main/client/src/pages/EvaraTankAnalytics.tsx', c); console.log('Fixed tank card');
